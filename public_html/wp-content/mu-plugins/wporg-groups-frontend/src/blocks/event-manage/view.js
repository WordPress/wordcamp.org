/**
 * Event Manage block — view script entry point.
 *
 * Loads the event modal React app. The app self-initializes by listening
 * for clicks on [data-wporg-groups-modal] buttons and mounting into the
 * #wporg-groups-event-modal-root div rendered by the block.
 *
 * @package WordCamp\Groups\Frontend
 */

import 'leaflet/dist/leaflet.css';
import './event-modal-app';
